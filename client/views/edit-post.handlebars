<div class="post card">
  <div class="card-header">
    <h2>Edit Entry</h2>
  </div>
  <div>
    <form id="edit-post-form" class="card-body">
      <div class="form-group">
        <input type="hidden" name="post-id" value="{{post.id}}" />
      </div>
      <div class="form-group">
        <label class="form-label" for="post-title">Title:</label>
        <input type="text" name="post-title" value="{{post.title}}" class="form-input" />
      </div>
      <div class="form-group">
        <label class="form-label" for="post-body">Thoughts:</label>
        <textarea style="width: 50%;" name="post-body" class="form-input form-control">{{post.body}}</textarea>
      </div>

      <button type="submit" class="btn">Update</button>

      <button type="button" id="delete-btn" class="btn">Delete</button>
    </form>
  </div>
</div>

<script src="/javascript/edit.js"></script>
